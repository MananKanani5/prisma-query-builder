{
  "name": "prisma-qb",
  "version": "1.0.3",
  "description": "A lightweight, strict Prisma query builder that converts HTTP query parameters into safe where and orderBy objects. Zero dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "repository": {
  "type": "git",
  "url": "https://github.com/MananKanani5/prisma-qb"
  },
    "keywords": [
    "prisma",
    "prisma-query",
    "prisma-query-builder",
    "query-builder",
    "rest-api",
    "filter",
    "sorting",
    "search",
    "backend",
    "typescript",
    "node",
    "zero-dependencies"
  ],
  "author": "Manan Kanani",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
